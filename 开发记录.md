# VoiceAssist - 开发记录

> 每个开发阶段先在此处输出设计（类型/接口/布局/测试点），用户确认后再写代码。
> 创建日期：2026-07-12

---

## 阶段 1：存储层（JSON 文件读写）

**对应任务清单：** 第一阶段 1.1 ~ 1.8

### 1.1 这个阶段要做什么（一句话版）

让 Rust 后端能安全地读写三份数据文件（消息/收藏/设置）和一个音频目录，并在删除消息时正确处理"来源消息被引用"的关系。

### 1.2 数据放在哪（先讲清楚，因为它是后面所有模块的基础）

Tauri 给每个应用一个专属的本地数据目录，Windows 和 macOS 各自带：

- **Windows**: `C:\Users\<你>\AppData\Roaming\com.voiceassist.app\`
- **macOS**: `~/Library/Application Support/com.voiceassist.app/`

（这个 `com.voiceassist.app` 就是我们初始化项目时在 tauri.conf.json 里设的 identifier。）

在这个目录下，我们放四样东西：

```
<app_data_dir>/
├── messages.json      # 消息记录
├── favorites.json     # 收藏列表
├── settings.json      # 设置键值对
└── audio/              # 生成的音频文件
    └── <message_id>.mp3
```

**为什么放在这里、而不是软件安装目录？** 因为安装目录用户没写权限（Windows 的 Program Files 受保护），而应用数据目录是系统指定的"你的应用可以随便写"的地方，且每个用户独立。卸载软件时这个目录不删，能保住用户的历史消息。

**怎么拿到这个路径？** Tauri 提供 `app_data_dir()` API，跨平台自动给到正确路径，我们只管用。

### 1.3 一个关键技术点：原子写（为什么需要、怎么实现）

#### 问题

写 JSON 文件最朴素的做法是"打开→覆盖写→关闭"。但如果写到一半突然断电、或程序崩了，文件就只剩半截内容，下次启动时 JSON 解析失败，**用户所有消息记录就没了**。这对一个"沟通助手"是不可接受的——记录是用户辛苦打出来的。

#### 解法：先写临时文件，再替换

行业标准做法叫"原子写"（atomic write）：

1. 把完整新内容写到 `messages.json.tmp`（一个临时文件）
2. 调系统 API 把 `.tmp` **重命名**成 `messages.json` —— 重命名这一步在操作系统层面是原子的（要么成功、要么完全没发生），不会出现"重命名到一半"的状态

这样即使在第 1 步断了电，原来的 `messages.json` 还完好无损；最坏情况只是丢了一个临时文件，下次自动清理就行。

#### 具体实现

Rust 里用 `tempfile::NamedTempFile::new_in(dir)` 创建临时文件 → `write_all` 写内容 → `persist(final_path)` 替换原文件。`.persist()` 内部就是那下原子的 rename。10 行左右代码。需要引入 `tempfile` 这个 crate（Rust 的第三方库）。

> 对你说的：crate（木箱）就是 Rust 的第三方包，类似 npm 的 package。`tempfile` 是 Rust 生态里专门管临时文件的最流行库。

### 1.4 文件布局（Rust 端会建哪些模块）

对应大纲 [4.1 目录结构](项目大纲.md) 里的 `src-tauri/src/storage/`：

```
src-tauri/src/
├── main.rs                       # 已有：入口
├── lib.rs                        # 已改：注册命令
├── storage/
│   ├── mod.rs                    # 模块入口 + 数据目录初始化
│   ├── types.rs                  # 数据结构定义（Message/Favorite/Settings）
│   ├── atomic.rs                 # 原子写工具
│   ├── audio_gc.rs               # 音频引用判断 + 无引用则删
│   ├── messages.rs               # messages.json 读写
│   ├── favorites.rs              # favorites.json 读写
│   └── settings.rs               # settings.json 读写
└── (后续 commands/ 在下一阶段接)
```

**为什么把"原子写"单独放一个文件？** 因为三份数据都要用它，集中写在 atomic.rs 里复用，改一处全改。

**为什么音频清理也单独放 `audio_gc.rs`？** 因为"判断一个音频是否还被引用"既要查 messages 又要查 favorites，是跨两份数据的逻辑；删消息和删收藏都要调它。集中放一处，两个删除流程共用同一套判断，不会出现"删消息时按一套规则、删收藏时按另一套规则"的不一致。

### 1.5 数据结构设计（types.rs）

用 Rust 的 struct 描述三份数据的形状，加上 `serde` 让它能和 JSON 互转。

```rust
// 一条消息
pub struct Message {
    pub id: String,               // "m_1690000000001"
    pub content: String,          // 输入的文本
    pub audio_path: String,        // 相对路径 "audio/m_169...mp3"
    pub created_at: String,        // ISO8601 时间戳字符串
}

// 一条收藏
pub struct Favorite {
    pub id: String,
    pub source_message_id: Option<String>,  // 来源消息 id；来源删除时变 None
    pub note: String,                        // 备注（必填，非空）
    pub audio_path: String,
    pub created_at: String,
}

// 设置（一个结构，序列化成 settings.json）
pub struct Settings {
    pub tts_engine: String,            // "mimotts"
    pub tts_model: String,             // "default"
    pub playback_volume: f32,          // 0.0~1.0 播放音量
    pub hotkey_show_window: String,    // "Alt+V"
    pub engine_category: String,       // "remote" / "local"
}
```

**几个设计点说明（对你解释下为什么这么定）：**

- **`id` 用时间戳+序号生成的字符串**（如 `m_1690000000001`），而不用数据库常见的自增数字 1/2/3。**核心动机是多窗口并发**：本软件有主窗+小窗两个同时活着的窗口,如果都用自增数字,两个窗口可能同时算出"下一条该是第 4 条",id 撞车。时间戳天然唯一、天然可排序、跨窗口不会撞。时间戳方案的唯一缺点是 id 比较长、不好记——但 id 是程序内部标识符、不给用户看,长一点无所谓。换设备/还原备份也不会重复,且 id 自带时间顺序、排序展示可直接用。我推荐它不是因为"酷",是因为它**在这种双窗口形态下更稳、更省心**。
- **`source_message_id` 类型是 `Option<String>`**：Rust 的 Option 表示"可能有、可能没有"。`Some("m_xxx")` 表示有来源，`None` 表示来源已被删除。这正好对应大纲里的"来源消息删除时置 null"。
- **`note` 是 `String` 而非 `Option<String>`**：备注必填，所以类型上就不允许为空。但 JSON 里存空字符串 `""` 也不合规——这点要在写入时校验（非空），靠代码保证而不是类型。
- **`audio_path` 存相对路径**（`audio/m_169...mp3`）而不是绝对路径。好处：以后数据目录搬家（换电脑、备份还原）路径不会失效。

**时间戳怎么来、id 怎么生成？** 大纲环境里我（Claude）不能直接用系统时钟（被限制），但你的 Rust 程序运行在用户机器上，可以用标准库 `std::time` 拿到当前时间生成 id 和时间戳。这一段我在写代码时会用 Rust 的 `chrono` crate（处理时间的库）正常实现。

### 1.6 读写函数清单

每个数据文件对外暴露 3 个左右函数：

```rust
// messages.rs
load_messages(dir) -> Vec<Message>                    // 读全部；文件不存在返回空
add_message(dir, message) -> Result<()>                // 追加一条 + 原子写回
delete_message(dir, id) -> Result<bool>                // 删一条 + 连带(收藏置None + 音频无引用则删)

// favorites.rs
load_favorites(dir) -> Vec<Favorite>
add_favorite(dir, favorite) -> Result<()>
delete_favorite(dir, id) -> Result<bool>               // 删一条 + 音频无引用则删
unlink_favorites_by_message(dir, message_id) -> Result<()>  // 删消息时把收藏来源引用置 None

// audio_gc.rs —— 音频引用计数/清理（新增模块，集中放"无引用则删"逻辑）
is_audio_referenced(dir, audio_path) -> bool          // 扫 messages + favorites 两份 JSON
maybe_delete_audio(dir, audio_path) -> Result<()>      // 无引用才删；文件不存在忽略

// settings.rs
load_settings(dir) -> Settings                         // 文件不存在/缺字段时返回默认值
save_settings(dir, settings) -> Result<()>
update_setting(dir, key, value) -> Result<Settings>     // 改一个键并返回完整 settings

// atomic.rs
atomic_write(path, content) -> Result<()>               // 核心原子写工具

// storage/mod.rs
data_dir(app) -> PathBuf                                // 封装 tauri app_data_dir
ensure_dirs(dir) -> Result<()>                          // 启动时确保目录和 audio/ 存在
```

### 1.7 删除消息的连带逻辑（1.7 任务的核心）

**音频删除原则（用户拍板）：音频若无任何引用则删,有引用则保留。** 即一条音频对应的来源消息已被删除、且没有任何收藏直接引用它,才删该音频文件;只要二者之一还在,音频就保留。

```
delete_message(dir, id):
  1. 读 messages.json，找到要删的那条 message（含其 audio_path）
     → 找不到则直接返回 false
  2. 从 messages.json 过滤掉该条，原子写回 messages.json
  3. 调 unlink_favorites_by_message(dir, id)
     → 读 favorites.json → 把所有 source_message_id == id 的改成 None → 原子写回
     → 同时收集"被改动的收藏"的 audio_path（这些收藏引用的是别的音频，与本消息音频无关，仅作记录，这里不删）
  4. 判断本消息的 audio_path 是否还被引用：
     - 扫描 messages.json 剩余消息：是否还有条目的 audio_path == 本音频？
     - 扫描 favorites.json：是否有任何收藏的 audio_path == 本音频？
     （注意：本软件约定一条消息音频与一条收藏音频是各自独立的文件，
      因此第 3 步把收藏的 source_message_id 置 None 后，收藏的 audio_path 仍指向其自身拷贝，
      不会与本消息的 audio_path 相同。扫描两份 JSON 是双保险。）
  5. 若两处都没有引用 → 删除磁盘上的音频文件（用 std::fs::remove_file，文件不存在则忽略）
  6. 返回 true（确实删了一条消息）
```

**顺序敏感性（为什么这么排）：**

- 必须先写回 JSON（步骤 2、3）成功，再删音频（步骤 5）。反过来的话，万一音频删了但 JSON 写回失败，会出现"音频没了但消息记录还在"——用户点播放会黑屏。先落数据、再清文件，最坏情况是"JSON 已删但音频文件残留占点磁盘"，这个无害、可日后清理；反之不可接受。
- 收藏删除（`delete_favorite`）走同样原则：删收藏记录后，检查该收藏的 audio_path 是否还被任何消息/其它收藏引用，无引用才删音频。这条在本阶段一并实现（属于 `favorites.rs` 的 `delete_favorite`）。

> 之所以"无引用才删"要扫两份 JSON，是因为音频路径可能同时出现在 messages.json（作为消息产物）和 favorites.json（作为收藏导入的音频）。两个文件都查一遍，才能确定"真的没人用了"。

### 1.8 单元测试（1.8 任务）

测试放在各模块同目录的 `#[cfg(test)] mod tests` 里（Rust 惯例）。要点：

- **原子写**：正常写入能读回、写入失败不影响原文件
- **messages**：追加顺序正确、删除已存在/不存在的 id 行为正确
- **favorites 连带**：删一条消息后，引用它的收藏 `source_message_id` 变 None，收藏本身数量不变
- **音频无引用则删（重点测）**：
  - 删一条消息，其音频没有被任何收藏引用 → 音频文件被删
  - 删一条消息，其音频被某收藏引用（导入过同一音频的极端场景）→ 音频保留
  - 删一条收藏，其音频未被别处引用 → 音频被删
  - 删一条收藏，其音频还被别的收藏/消息引用 → 音频保留
  - 删消息时先写回 JSON 再删音频的顺序：用"故意制造音频删除失败"的场景验证 JSON 已落地（即音频删不掉不会回滚 JSON，数据仍一致）
- **settings**：文件不存在返回默认值、缺字段返回默认值、增量改一个键不丢其它键
- 用临时目录跑，测完自动清理（`tempfile::tempdir`）；音频测试在临时目录里建假音频文件验证

### 1.9 引入的依赖（Cargo.toml 要加的东西）

- `serde` / `serde_json`：序列化（骨架已带）
- `tempfile`：原子写用（新增）
- `chrono`：时间戳/id 生成用（新增）
- 生产依赖不加数据库相关东西（坚持 JSON 方案）

### 1.10 这个阶段**不做**的事（边界要划清）

- ❌ 不接 TTS、不生成音频（下一阶段 2.x）
- ❌ 不暴露 Tauri 命令给前端（下一阶段 3.x，commands/ 层）
- ❌ 不做前端 UI 绑定（前端后续按需求再定）

这个阶段只做出"纯 Rust 能读写三份数据 + 音频无引用则删"这一层，外加单元测试验证。音频清理已纳入本阶段（用户拍板）。

---

## 阶段 2：TTS 引擎抽象层 + MiMo 实现

**对应任务清单：** 第一阶段 2.1 ~ 2.5

**MiMo 形态已核实**（来源 [mimo-tts-v2.5.md](mimo-tts-v2.5.md)），以下设计基于文档事实而非推测。

### 2.1 先讲清楚 MiMo 的真实形态（核实结论）

| 维度 | 实际情况 |
|---|---|
| 协议 | HTTP POST `https://api.xiaomimimo.com/v1/chat/completions` |
| 鉴权 | 请求头 `api-key: <key>`(**非 OpenAI 的 Authorization Bearer**) |
| 首版模型 id | `mimo-v2.5-tts`(预置音色);另两个 voicedesign/voiceclone 本阶段不做 |
| 入参 | 类 chat completions:要合成的文本放 `role:"assistant"` 的 content(反直觉,但 MiMo 借了 LLM 的壳);`role:"user"` 放风格指令(本阶段留空) |
| 音色 | `audio.voice`:首版给用户选,默认 `mimo_default`,可选 冰糖/茉莉/苏打/白桦/Mia/Chloe/Milo/Dean |
| 音频格式 | **首版用非流式 wav**(`audio.format="wav"`);流式 pcm16 拼接暂不做 |
| 返回 | JSON:`completion.choices[0].message.audio.data` 为 **base64 编码的 wav 字节**,解码后写文件 |
| 计费 | 限时免费 |

**为什么用非流式 wav**:流式要边收 PCM 边拼、再封 wav 头,代码复杂且容错点更多;非流式一次拿完整段 wav 实现 30 行内,首版用它,流式作为后续 trait 扩展位。

### 2.2 本阶段能力边界（用户拍板）

- ✅ 仅 `mimo-v2.5-tts` 模型 + **预置音色** + **纯文本合成**(无风格指令)
- ✅ API Key **明文**存 settings.json(加字段 `mimo_api_key`)
- ❌ 不做风格指令(自然语言/音频标签都不做)
- ❌ 不做 voicedesign / voiceclone 两模型
- ❌ 不做流式调用
- 接口设计上**留扩展位**:trait 参数结构体预留 voice/instruction 等,现在填空、以后填上即可

### 2.3 引擎抽象（`tts/traits.rs`）

```rust
pub struct TTSParams<'a> {
    pub text: &'a str,          // 要合成的文本
    pub voice: Option<&'a str>, // 音色 id；None 用引擎默认
    pub instruction: Option<&'a str>, // 风格指令；本阶段永远 None（扩展位）
}

pub trait TTSEngine: Send + Sync {
    /// 合成语音。返回音频文件的相对路径(相对 app_data_dir,如 "audio/m_xxx.wav")
    async fn generate(&self, params: TTSParams<'_>) -> Result<String>;
    /// 引擎名("mimotts")
    fn name(&self) -> &str;
    /// 引擎类别
    fn category(&self) -> EngineCategory;
}

pub enum EngineCategory { Local, Remote }
```

**为什么参数包成 `TTSParams` 结构体**:将来加 `instruction`(风格指令)只往结构体加字段,不用改每个函数签名——这正是 v1.1 评审里 [后续待定.md](后续待定.md) #1 提到的"参数扩展"问题,这次先把壳留好,以后填字段不破坏调用方。

### 2.4 MiMo 实现（`tts/mimo.rs`）

构造一个 `MimoEngine { api_key, base_url, model, data_dir }`。核心流程:

```
generate(TTSParams { text, voice, instruction: None }):
  1. 生成 id (复用 storage::types::gen_id("m"))
  2. 组请求体:
     {
       "model": "mimo-v2.5-tts",
       "messages": [
         { "role": "user", "content": "" },                  // 本阶段留空
         { "role": "assistant", "content": <text> }          // 合成文本放这
       ],
       "audio": { "format": "wav", "voice": <voice 或 "mimo_default"> }
     }
  3. 用 reqwest POST 到 base_url,带 header:
     - api-key: <api_key>
     - Content-Type: application/json
  4. 拿响应 JSON,取 choices[0].message.audio.data (base64)
  5. base64 解码 → 写到 <data_dir>/audio/<id>.wav (原子写复用 storage::atomic)
  6. 返回相对路径 "audio/<id>.wav"
```

**几个细节**:

- **超时**:reqwest 设 30 秒。MiMo 合成短句通常几秒,30 秒兜底防网络挂死(呼应"响应速度快"非功能需求——超时能让前端及时给提示而非干等)
- **user 消息留空字符串**:MiMo 文档("user 消息为可选参数"),首版不传风格指令就传 `""`。voicedesign 要求 user 必填,本阶段不做那模型所以无碍
- **音频文件扩展名用 `.wav`**:MiMo 返回 wav,存 wav;以后若加 pcm16 流式再单独命名

### 2.5 错误处理（`tts/` 用 anyhow 还是自定义 Error）

用一个统一的 `TtsError` 枚举,分门别类,让上层翻译成中文提示:

```rust
pub enum TtsError {
    NoApiKey,              // settings 里没配 key
    Network(String),       // reqwest 失败(HTTP 连不上)
    Http { status: u16, body: String }, // 返回非 200(如 key 错 401、欠费)
    DecodeAudio(String),   // base64 解码失败
    WriteFile(String),     // 写文件失败
}
```

对应中文提示举例(在 commands 层翻译,本层只抛类型):
- `NoApiKey` → "请在设置中填写 MiMo API Key"
- `Http 401` → "API Key 无效，请检查"
- `Http 4xx/5xx` → 取服务端返回的 message 给用户看

### 2.6 引擎选择（`tts/mod.rs`）

```rust
// 根据 settings.tts_engine 字段返回引擎实例(首版只支持 "mimo")
pub fn build_engine(settings: &Settings, data_dir: &Path) -> Result<Box<dyn TTSEngine>> {
    match settings.tts_engine.as_str() {
        "mimo" => Ok(Box::new(MimoEngine::new(settings.mimo_api_key.clone(), data_dir))),
        other => Err(TtsError::UnknownEngine(other.into())),
    }
}
```

**为什么用 `Box<dyn TTSEngine>`**(对用户解释):这是 Rust 里"一个变量可以指向不同的具体引擎"的写法。首版只有 MiMo,但加第二个引擎(比如本地 sherpa)时,这里加一个 match 分支即可,上层代码一字不改——这就是 v1.1 评审 #8 待定的"动态加载"该怎么做的雏形,先这么用。

### 2.7 与存储层的衔接（重要）

TTS 本身只**生成音频文件并落地**,不写 messages.json!**写 messages.json 是下一阶段 commands 层的事**(任务 3.1)。理由:

- TTS 引擎只管"文本→音频文件",职责单一
- "把这条消息记进 messages.json"是上层应用逻辑(命令层决定要不要记、记什么时间戳)
- 这样 TTS 层可单独测试(不依赖消息存储),也方便以后在 commands 层加"预览不记录"等开关

### 2.8 测试策略（关键:不能在没有真 key 时跑通不了就卡住）

- **单元测试不调真实 MiMo API**(网络依赖、要 key、CI 跑不了)。MimoEngine 内部把"构造请求→发 HTTP→解析返回"这串拆开,只测能测的部分:
  - 测"请求体构造":给一段文本+音色,断言生成的 JSON 结构正确(text 放 assistant、voice 放 audio)
  - 测"返回解析":喂一个**假的成功响应 JSON**(含 base64 编的一段假 wav 字节)、断言能正确解出字节并写到文件
  - 测 NoApiKey 报错:构造空 key 的 engine,断言 generate 立即返回 NoApiKey
- **真实联调用一个 `#[ignore]` 的集成测试**(标了 ignore 的测试默认不跑,只有手动 `cargo test -- --ignored` 才跑),需要环境变量 `MIMO_API_KEY`,你真要连通时再跑。这样**:仓库默认测试仍不依赖网络/key,你拿到 key 想验通就单跑那个**

### 2.9 引入的依赖（Cargo.toml 要加）

- `reqwest` (含 `json` feature,默认 rustls-tls,避免 OpenSSL 依赖) —— HTTP 请求
- `base64` —— 解码 MiMo 返回的音频
- `tokio` 已由 reqwest/tauri 间接拉入,无需直接加

**为什么 reqwest 用 rustls 而非 native-tls**:跨平台打包时,rustls 不依赖系统的 OpenSSL/Schannel,Win 和 Mac 都不会因为 TLS 库链不上出"链接错误"。这正是 v1.1 大纲里点名担心的 SQLite 类坑,我们在 HTTP 库上提前规避。

### 2.10 本阶段**不做**的事（边界）

- ❌ 不暴露 Tauri 命令给前端(下一阶段 3.x)
- ❌ 不写 messages.json(TTS 只产音频,记录逻辑留给 commands 层)
- ❌ 不做风格指令 / voicedesign / voiceclone / 流式
- ❌ 不做前端 UI 绑定

这个阶段产出"调 MiMo → 落 wav 文件 + 返回相对路径"这一层,加可测性好的单元测试 + 一个手动联调的 ignored 集成测试。

---

---

## 阶段 3：Tauri 命令层（前后端桥接）

**对应任务清单：** 第一阶段 3.1 ~ 3.5

**目标**：在 Rust 后端和前端之间搭起 Tauri 命令桥——把前端的"发消息""删消息""读消息""读设置"等操作，通过 Tauri 命令暴露给前端调用，同时让后端能够在发生数据变化时广播事件通知前端。

### 3.1 现存的基础

在开写前先确认手里已经有什么：

- ✅ `storage/` —— 读写 messages.json / favorites.json / settings.json，含原子写、音频无引用则删
- ✅ `tts/` —— `MimoEngine::generate()` 生成音频并返回相对路径
- ✅ 依赖已配好（serde / reqwest / chrono 等）

### 3.2 要解决的问题（对你解释）

现在后端有两个能力：「存储层」能读写数据、「TTS 引擎」能合成语音——但它们都不在前端能调用的范围。

`commands/` 层要做的事就是**把后端的函数变成前端可以调用的命令**。你可以把它理解成一个菜单——你在前端点"生成语音",这个命令传到 Rust 里 TTS-> 写 messages.json-> 广播"数据变了",最后前端看到结果。

流程非常直接：

```
前端点"发送"
  ↓ 调 Tauri 命令
commands/tts.rs::generate_tts(text)
  ↓ 调 TTS 引擎    → 生成 audio/xxx.wav
  ↓ 写 messages.json → 存进存储层
  ↓ 广播事件        → "message:changed"
  ↓ 返回 Message    → 前端拿到数据更新界面
```

### 3.3 共享状态（AppState）

多个命令需要共享的数据放在一个 `AppState` 里，通过 Tauri 的 `manage()` 注入，命令通过 `State<'_, AppState>` 访问。

```rust
pub struct AppState {
    pub data_dir: PathBuf,
    pub settings: RwLock<Settings>,         // 读多写少，用读写锁保护
}

impl AppState {
    pub fn new(data_dir: PathBuf, settings: Settings) -> Self {
        Self { data_dir, settings: RwLock::new(settings) }
    }
    /// 获取 settings（读锁，多个线程可同时读）
    pub fn read_settings(&self) -> parking_lot::RwLockReadGuard<'_, Settings> {
        self.settings.read()
    }
    /// 获取 settings 写锁
    pub fn write_settings(&self) -> parking_lot::RwLockWriteGuard<'_, Settings> {
        self.settings.write()
    }
}
```

**TTS 引擎不放在 AppState 里**——MimoEngine 只是几个字符串（api_key / base_url / model），每次调 `build_engine` 构造非常轻量（不涉及网络连接）。这样简化并发处理，避免跨线程传递引擎实例的生命周期问题。

**为什么不用标准库 RwLock 而用 parking_lot？** parking_lot 不依赖操作系统，跨平台一致（标准库 RwLock 在 Windows 上用 SRWLOCK 某些场景有读者饿死问题）。Cargo.toml 加 `parking_lot = "0.12"`。

### 3.4 命令签名总览

所有命令都是 `async fn`，通过 `#[tauri::command]` 暴露给前端。

```rust
// sync.rs —— 事件常量与广播工具
pub const EVENT_MESSAGE_CHANGED: &str = "message:changed";
pub const EVENT_FAVORITE_CHANGED: &str = "favorite:changed";
pub const EVENT_SETTINGS_CHANGED: &str = "settings:changed";

/// 向所有窗口广播事件（payload 只含 "reload" 信号，不含完整数据——窗口自己重读）
pub fn notify_changed(app: &AppHandle, event: &str) -> Result<()>;
```

```rust
// commands/tts.rs
#[tauri::command]
pub async fn generate_tts(
    text: String,
    app: AppHandle,
    state: State<'_, AppState>,
) -> Result<Message, String>;

// commands/message.rs
#[tauri::command]
pub async fn list_messages(state: State<'_, AppState>) -> Result<Vec<Message>, String>;
#[tauri::command]
pub async fn delete_message(id: String, state: State<'_, AppState>) -> Result<bool, String>;

// commands/favorite.rs
#[tauri::command]
pub async fn list_favorites(state: State<'_, AppState>) -> Result<Vec<Favorite>, String>;
#[tauri::command]
pub async fn add_favorite(source_msg_id: Option<String>, note: String, state: State<'_, AppState>) -> Result<Favorite, String>;
#[tauri::command]
pub async fn delete_favorite(id: String, state: State<'_, AppState>) -> Result<bool, String>;

// commands/settings.rs
#[tauri::command]
pub async fn get_settings(state: State<'_, AppState>) -> Result<Settings, String>;
#[tauri::command]
pub async fn update_setting(key: String, value: serde_json::Value, state: State<'_, AppState>) -> Result<Settings, String>;
```

> **为什么返回 `Result<_, String>` 而不是 `Result<_, TtsError>`**：Tauri 命令的返回需要实现 `Serialize`（用于跨 FFI 传给前端）。`String` 类型天然序列化，错误消息直接传给前端。实际使用时分两种情况：(1) 用户可理解的错误（如"API Key 未配置"）(2) 内部错误（如写入失败），前者前端展示，后者也展示但可加日志。首版全用 `Result<_, String>` 简化。

### 3.5 generate_tts 完整流程（核心流程）

```
generate_tts(text):
  1. 读取 settings（app_state 读锁）
     → api_key 为空 → 返回 "请在设置中填写 MiMo API Key"
  2. 构建 MimoEngine（settings.mimo_api_key, data_dir）
  3. 调用 engine.generate(TTSParams::new(text))
     → 返回 audio_path（"audio/m_xxx.wav"）
  4. 构造 Message 对象（id/gen_id, created_at/now_iso）
  5. 调 storage::messages::add_message 写入 messages.json
  6. 调 notify_changed(app, EVENT_MESSAGE_CHANGED)
  7. 返回 Message（给前端加消息气泡 + 本地播放音频路径）
```

### 3.6 收藏"导入音频"的说明

收藏有两类来源：

**① 从消息气泡右键"收藏"**（本阶段实现）：
- 前端传 `source_message_id` 和 `note`
- 后端找到原消息，把其音频复制一份到 `audio/f_<id>.wav`
- 写 favorites.json，`source_message_id` 指向原消息，`audio_path` 指向新拷贝
- **音频拷贝而非引用**：保证原消息删除后收藏音频不受影响

**② 外部导入音频文件**（P1 任务 8.2，本阶段不做）：
- 用户选择本地文件，复制到 `audio/` 下，填备注
- `source_message_id` = None
- 本阶段不实现，接口已预留

### 3.7 setup 与状态初始化

Tauri 启动时（`lib.rs` 的 `setup` 回调）做：

```rust
.setup(|app| {
    let data_dir = app.path().app_data_dir()?;
    storage::ensure_data_dirs(&data_dir)?;
    let settings = storage::settings::load_settings(&data_dir);
    app.manage(AppState::new(data_dir, settings));
    Ok(())
})
```

启动时 settings 不一定要常驻内存，但本阶段保持"启动时读一次到 AppState，更新时同时写文件 + 改 AppState"的方式，避免每次读文件的开销（虽然文件本身体积不大，但这是一种工程惯例）。

### 3.8 前端侧：tauri API 调用封装

前端会通过 `@tauri-apps/api/core` 的 `invoke()` 调用这些命令。封装在 `src/services/` 下：

```typescript
// services/tts.ts
export async function generateTTS(text: string): Promise<Message> {
  return invoke<Message>("generate_tts", { text });
}

// services/messages.ts
export async function listMessages(): Promise<Message[]> {
  return invoke<Message[]>("list_messages");
}

// services/events.ts
import { listen } from "@tauri-apps/api/event";

export function onMessageChanged(callback: () => void): Promise<() => void> {
  return listen<void>("message:changed", callback);
}
```

> 这些封装的 TypeScript 文件将在阶段 4（前端组件）中具体用到。本阶段只确认它们的接口签名。

### 3.9 本阶段**不做**的事（边界）

- ❌ 不做前端 UI 组件（阶段 4 的事）
- ❌ 不做收藏"导入外部音频"（P1 8.2）
- ❌ 不做 events.ts 的 listen 绑定（阶段 4 做）
- ❌ 不做 TTS 引擎本地兜底（后续待定项）

这个阶段产出：Rust 侧全部命令实现 + Tauri 注册 + setup 初始化 + 后端编译通过 + 测试验证。

---

*阶段 3 设计稿提交时间：2026-07-13，待用户确认后进入实现*

---

## 阶段 4：前端主界面聊天组件（一次性打通 MVP）

**对应任务清单：** 第一阶段 4.1 ~ 4.7

**特殊说明**：因核心后端（存储 + TTS + 命令）阶段 3 已全部完成，本阶段直接一次性实现，让用户 `npm run tauri dev` 即可看到打字→发送→听语音的完整体验。

### 4.1 布局（App.tsx）

```
┌─────────────────────────┐
│ 标题栏：VoiceAssist       │
├─────────────────────────┤
│ 消息列表（可滚动）        │  ← 各消息气泡，右键可删/收藏
│ 自身消息靠右（蓝色气泡）  │
├─────────────────────────┤
│ 工具栏：🔊 音量滑块        │  ← playback_volume
├─────────────────────────┤
│ [输入框......] [发送]    │  ← 回车发送
└─────────────────────────┘
```

### 4.2 组件拆分

- App.tsx —— 顶层布局 + 状态协调
- ChatWindow —— 消息列表 + 启动加载 + 监听重构事件
- MessageBubble —— 气泡显示 + 播放按钮 + 右键菜单
- InputBox —— 输入框 + 发送
- VolumeSlider —— 播放音量调节

### 4.3 数据流（端到端）

```
回车发送
  ↓ invoke("generate_tts", { text })
  ↓ 后端：TTS→写 messages.json→广播 message:changed
  ↓ 返回 Message（前端立即加一个气泡）
  ↓ 自动播放该条音频
  ↓ 同时（双保险）message:changed 触发 list_messages 重读
```

**双刷新策略**：命令直接返回的 Message 立即加气泡（用户感知快），事件触发整表 reload（跨窗口一致）。

### 4.4 音频播放

- Window 自定义协议 `audio://<相对路径>` → Rust 揑楼返回文件字节（本阶段集成）
- 或简单方案：`convertFileSrc` 将相对路径转 `asset://` URL，赋给 `<audio>` 元素
- 播放音量用 `settings.playback_volume`，每次播放前更新 `audioElement.volume`

### 4.5 右键菜单（ContextMenu）

用浏览器原生 `onContextMenu` 简单实现：右键弹原生菜单显示"删除""收藏"，收藏弹一个简单的 prompt 输入备注（后续阶段可换成自定义 Modal）。

### 4.6 状态管理（Zustand）

- messageStore：messages 数组 + add/set/clear
- settingsStore：settings + update

本阶段前端规模不大，Zustand store 非必需，可用 React useState。但为后续阶段（浮窗、收藏夹）连贯，建一个最简的 settingsStore 即可。

### 4.7 边界

- ❌ 不做浮窗（P1）
- ❌ 不做收藏夹界面（P1 8.x）
- ❌ 不做设置界面（P1 9.x）——api_key 等可通过 `update_setting` 命令手动 set，或先丢一个最小设置 UI
- ✅ 主界面能用、能听语音、能删/收藏（收藏先弹简单 prompt）
- 首版如果未配 key，发送会弹"请在设置中填写 MiMo API Key"——此阶段顺便补一个最小 API Key 设置入口

---

*阶段 4 设计稿提交时间：2026-07-13，直接进入实现以打通 MVP*

---

## 阶段 4.5：播放速度 + 合成语速 + 自动播放延迟（修整）

**用户反馈触发**：MVP 跑通后三条小改进。

### 4.5.1 自动播放延迟 0.2 秒（前端纯改）

`App.tsx::handleSend` 中 `playAudio(msg.audio_path)` 改为 `setTimeout(() => playAudio(msg.audio_path), 200)`。理由：用户反馈直接播有点突兀，0.2s 给人"已发出"的感知缓冲。

### 4.5.2 播放速度调节 A（前端纯改）

- HTMLAudioElement 内置 `playbackRate`（0.5~2.0），原生支持，精确。
- Settings 加 `playback_rate: f32`（默认 1.0）
- 加一个"语速"小滑块在工具栏内
- `MessageBubble` 和自动播放在 `play()` 前设 `audio.playbackRate = playback_rate`

### 4.5.3 合成语速调节 B（后端 + 前端）

**MiMo 文档核实结论**：MiMo 没有数值 `speed` 字段，语速只能通过自然语言风格指令（user 消息）控制——这是"伪精确"。

实现方式（用户选的数值滑块方案）：

- Settings 加 `tts_speed: f32`（0.5~2.0，默认 1.0）
- 前端工具栏第二个滑块"🚀 语速"，调它即调合成语速
- `generate_tts` 命令多接一个 `speed: Option<f32>` 参数（前端把当前 settings.tts_speed 传进去）
- 后端构造请求体：当 speed ≠ 1.0，在 user 消息里塞 `请以约 {speed} 倍速度朗读`
- TTSParams 加 `instruction: Option<&str>` 字段（设计稿 2.3 预留的扩展位，现在用上）
- MimoEngine::build_request_body 把 instruction 填进 `messages[0].content`（user 角色）

**伪精确性告知**：拖到 1.3x，MiMo 收到"以约 1.3 倍速度朗读"，理解成什么样由模型定，不保证精确 1.3 倍。这是 MiMo 限制，不是 bug。

---

*阶段 4.5 设计稿提交时间：2026-07-13，直接实现*

---

## 阶段 4.6：悬浮球音量控件 + 互斥播放 + 删合成语速（修整）

### 4.6.1 删除合成语速（#1）

- 删 Settings 的 `tts_speed` 字段、`commands/tts.rs` 里的 instruction 构造、`mimo.rs` build_request_body 的 instruction 参数退回 None（恢复 user 消息为空 ""）
- 前端工具栏删 `TtsSpeedPicker`/相关
- `TTSParams.instruction` 字段保留（扩展位，留作以后用），但不再从外部填内容
- ⚠ 不破坏测试：`build请求体_带风格指令` 这条测试改成测"传 None 时 user content 为空"，或保留测 instruction 字段本身仍能用——选后者（trait 接口不动，只是上层不传）

### 4.6.2 音量+播放速度合并到悬浮球（#2）

#### 状态机

```
            ┌─ mouseenter ─→ EXPANDED（滑块展开）
  COLLAPSED ─┤
  （球）     └─ mousedown 持续 ≥ 300ms 不松 ─→ DRAGGING（滑块收回，可拖动）
                  ↑ 松开 mouseup → 回到 COLLAPSED，球停在松开位置
```

#### 交互细节

- **默认**：一个圆形小球（约 36px），半透明蓝，浮在界面右上角（默认位置）
- **hover（mouseenter）**：球旁边弹出一个小卡片，含音量🔊 + 播放速度⚡ 两个滑块。鼠标移出球+卡片区域 → 收回
- **长按拖拽**：在球上按下鼠标保持 ≥300ms 不松 → 进入拖拽模式，卡片强制收回，鼠标移动即移动球。**松开后球就停在新位置，回到 COLLAPSED**
- **短按**（按下到松开 <300ms）：不触发拖拽；松开会自然算"hover 中"保持展开（因为鼠标还在球上）
- **拖拽边界**：球的中心点限制在窗口可视区域内（用窗口 clientWidth/Height 约束）
- **持久化位置**：球的位置（x, y 相对窗口）存 settings，重启还原（先不存也行，用户说"限定在界面内"未要求记忆；首版**先不持久化**，固定右上角，需要再做）

#### 悬浮球与互斥播放的关系

球的拖拽用 mousedown/mousemove/mouseup 实现。与音频播放完全独立。

### 4.6.3 自动播放延迟 0.4 秒（#3）

`App.tsx::handleSend` 的 `setTimeout(() => playAudio(...), 200)` 改 `400`。

### 4.6.4 互斥播放（#4）

#### 方案：共享单一 Audio 元素

- App 层持有一个全局 `Audio` 元素（现有 `playingRef`），所有播放（自动播放 + 手动点播放按钮）都走它
- `MessageBubble` 删掉自己的 `<audio>` 元素和 audioRef，点"▶ 播放"改为调父级传入的 `onPlay(audio_path)` 函数
- `onPlay` 实现：停掉当前 → 设新 src → 播放。天然互斥（只有一个 audio 元素）
- 当前正在播放的消息 id 状态放在 App，用于 UI 上"正在播放"高亮（可选，顺手做）

好处：天然互斥、音量/速度统一在一处应用、省去每个 bubble 一个 audio 元素。

### 4.6.5 状态变更

- Settings：删 `tts_speed`；`playback_volume` / `playback_rate` 保留
- 工具栏从"三块"缩为"悬浮球内两滑块"
- 后端 `generate_tts` 不再读 tts_speed

### 4.6.6 本阶段不做

- ❌ 球位置持久化（先固定右上）
- ❌ 球的"正在播放"脉冲动效（可选，先不做）
- ✅ 上述四条

---

*阶段 4.6 设计稿提交时间：2026-07-13，待确认 #2 交互后实现*

---

## 阶段 5：收藏夹界面（P1 8.x）

**对应任务清单：** 收藏夹界面 8.1 ~ 8.4

### 5.1 现存基础

- 后端 `favorites.json` 读写已就绪（阶段 1）
- `commands/favorite.rs` 已有三个命令：`list_favorites` / `add_favorite`（从消息收藏）/ `delete_favorite`
- 主界面右键消息"收藏"已能写进收藏

**缺**：收藏夹的**展示界面**和**导入外部音频**命令。

### 5.2 呈现方式（需用户拍板）

收藏夹和消息列表是两个独立视图，但它俩共用同一个窗口。三个方案：

**A. 顶部 tab 切换（推荐）**：标题栏下方加"消息 | 收藏"两个 tab，点哪个切哪个。单窗口、切换快、最简单。

**B. 独立窗口**：开一个新窗口显示收藏。任务清单 9.x 浮窗也是新窗口，易混。多一层窗口管理。

**C. 侧边栏**：左侧栏放收藏列表，右侧是消息。窄窗口（380px）不适合侧栏，挤。

### 5.3 收藏夹列表（8.1）

```
┌─────────────────────────┐
│ [消息] [收藏]            │ ← tab
├─────────────────────────┤
│ ⭐ 队友问号时的回应    ▶ │ ← 备注文本 + 播放按钮 + ... 菜单
│ ⭐ 胜利台词            ▶ │
│ ...                     │
├─────────────────────────┤
│      [ + 导入音频 ]      │ ← 底部导入按钮
└─────────────────────────┘
```

- 每条：备注（必填、非空）+ ▶ 播放 + 右键/... 菜单（删除）
- 播放复用主界面全局 audio（互斥：点收藏的音频会停掉消息正播的）
- 空态提示"暂无收藏，右键消息可收藏，或点下方导入音频"

### 5.4 导入外部音频（8.2）

需要后端新增命令 + 前端 dialog：

**后端**：`commands/favorite.rs` 加 `import_favorite(file_path, note)` 命令：
1. 接收用户选的绝对路径（如 `D:\xxx\clap.mp3`）
2. 生成收藏 id `f_xxx`
3. 把文件**复制**到 `<data_dir>/audio/f_xxx.<原扩展名>`
4. `source_message_id = None`，`audio_path = audio/f_xxx.mp3`
5. 写 favorites.json，返回 Favorite

**前端**：用 Tauri 的 dialog 插件弹文件选择器，选完调 `import_favorite`。备注必填用 prompt（首版）或弹窗。

**依赖**：加 `tauri-plugin-dialog`（文件选择）。Cargo.toml + capability 权限。

### 5.5 播放与互斥

收藏夹和消息列表**共享同一个全局 Audio**（App 持有 playingRef）。点收藏的音频播放时，消息正播的会被停，反之亦然。`playingId` 状态需要在两个视图间共享——但 playingId 存的是消息 id，收藏夹里用收藏 id 区分。改为存 `(类型, id)` 二元组或用 audio_path 作 key。

### 5.6 删除收藏（8.3）

右键/... 菜单调 `delete_favorite`，后端已实现（含音频无引用则删）。

### 5.7 入口与切换（8.4）

tab 在标题栏下方。切到"收藏"时调 `list_favorites` 加载。两个视图各自维护自己的数据加载，share 全局播放。

### 5.8 本阶段不做

- ❌ 收藏夹的多选/批量删除（首版单条删）
- ❌ 收藏排序/搜索（后续待定项已说不做搜索）
- ❌ 备注 编辑（导入后备注不可改，非要改只能删了重建）
- ✅ tab 切换、列表、播放、删除、导入

---

*阶段 5 设计稿提交时间：2026-07-13，待用户确认 #5.2 呈现方式后实现*

---

## 阶段 5.1：右键"在文件夹中打开"（顺带改进）

**触发**：用户要求消息/收藏的音频均能右键"在资源管理器打开"。

- 复用 `tauri-plugin-opener` 的 `revealItemInDir(absPath)`，需绝对路径
- 前端 services 加 `getAudioAbsPath(relPath)`（同后端 `resolve_audio_url` 命令但不经 `convertFileSrc`）
- 消息气泡右键菜单加一项「📁 在文件夹中打开」
- 收藏列表每条加一个 📁 按钮（hover 出现，与 🗑 并列）
- opener:default 权限已具备，无需改 capability

---

## 阶段 6：设置界面（P1 9.x）

**对应任务清单：** 9.1 ~ 9.4

### 6.1 当前状态

- 已有 `ApiKeyModal`（最小入口），存 settings 的 `mimo_api_key`
- 全部设置可走 `get_settings` / `update_setting` 命令

### 6.2 设置项

首版设置界面包含：
- **MiMo API Key** 输入框（替换原 ApiKeyModal 的功能）
- **默认音色** 选择（预置音色下拉：mimo_default/冰糖/茉莉/苏打/白桦/Mia/Chloe/Milo/Dean）→ 存 `tts_model`
- **呼出浮窗快捷键**（P1 10.x 用，本阶段先把字段做出来，实际快捷键功能下一阶段实现）→ 存 `hotkey_show_window`
- **关于**：版本号、软件描述、MiMo 文档链接

### 6.3 呈现方式

- 设置按钮 ⚙️ 点击 → 弹一个**右侧抽屉**或**全屏覆盖**的设置面板（替代当前的 `showSettings` 一行）
- 抽屉式更贴近桌面应用习惯，做右侧从右滑入
- 底部"关闭"按钮或点抽屉外的遮罩收回

### 6.4 关于页（9.3）

合并到设置内最后一块，不单独立"关于"tab。显示：
- 应用名 VoiceAssist + 版本号（从 package.json 或 tauri.conf 读）
- 一句话描述
- 文档/开源链接（暂为占位，开源阶段补实际地址）

### 6.5 本阶段不做

- ❌ 快捷键实际注册（下一阶段 10.x）
- ❌ 引擎动态切换（首版只 mimo）
- ❌ 自动更新检查（P2）

---

*阶段 5.1 + 6 设计稿提交时间：2026-07-13，待确认 #6.3 呈现方式后实现（已确认）*

---

## 阶段 7：快捷输入浮窗 + 全局快捷键 + 跨窗口同步（Win 优先）

**对应任务清单：** 10.1 ~ 10.9 + 11.1 ~ 11.4

### 7.1 浮窗定位回顾（大纲 4.7）
- 轻量、用完即走的输入弹层，核心场景是全屏游戏/快速 TTS
- 与主窗共用同一份数据，调同一个 `generate_tts`
- 发送后仅显示"已发送"极简提示，不展示消息条/进度
- 右上角齿轮菜单藏"打开主界面"
- 不约束同屏（主窗显示时浮窗随时可弹）
- 发送完成后自带音频播放？设计稿记录"浮窗发送后仅显示已发送"——先不给浮窗自带播放（生成+自动播放留给主窗做优先，浮窗纯发）。浮窗要听效果可以去主窗。

### 7.2 技术架构

**两个窗口，同一份前端代码**：第二窗口 `quick_input` 同加载 `index.html`，React 运行时通过 `getCurrentWindow().label` 判定 UI。

```
main（主窗 380×600）          quick_input（浮窗 320×150）
  ┌──────────────────────┐     ┌──────────────────────┐
  │ 消息列表 + 输入框等   │     │   [输入栏.........][发送] │
  └──────────────────────┘     └──────────────────────┘
```

### 7.3 需要新增的依赖
- `tauri-plugin-global-shortcut`（全局快捷键）
- 前端 `@tauri-apps/plugin-global-shortcut`

### 7.4 窗口配置（tauri.conf.json）

```json
{
  "label": "main",
  "width": 380, "height": 600,
  "visible": true,
  "focus": true
},
{
  "label": "quick_input",
  "width": 320,
  "height": 150,
  "decorations": false,
  "alwaysOnTop": true,
  "visible": false,
  "focus": true,
  "resizable": false
}
```

### 7.5 全局快捷键注册（Rust setup）

Tauri 启动时注册 `Alt+V`（读 settings.hotkey_show_window），回调：
- 若 quick_input 已可见 → 隐藏（toggle）
- 若不可见 → 显示并聚焦

### 7.6 浮窗外点击自动隐藏（11.4）

监听 `on_window_event(Focused(false))` → 隐藏。松耦合，不阻塞其它自动关闭行为。

### 7.7 浮窗组件

一个纯前端组件 `QuickInput`：
- 极精输输入框 + 发送按钮（与主窗 InputBox 类似，但去掉消息列表等所有多余 UI）
- 发送成功 → 显示"已发送"0.8秒 → 恢复输入态
- 右上角齿轮图标 → 下拉菜单：打开主界面 / 快捷键设置入口
- 自动聚焦输入框（显示时 autoFocus）

### 7.8 app.tsx 多窗口路由

```tsx
const label = getCurrentWindow().label;
if (label === "quick_input") return <QuickInput />;
else return <MainUI />;
```

### 7.9 跨窗口同步（mechanism 已存在）

- `generate_tts` 写完消息后广播 `message:changed` — 已有
- 主窗监听 `message:changed` 刷新列表 — 已有（阶段 4）
- 浮窗不需要监听消息事件（它不展示列表）
- **聚焦兜底**：主窗 `onFocusChanged` 时重读 — 已有（阶段 4.6）

所以 sync 机制**基本全现成**，只需确认浮窗的焦点监听为主窗兜底也正常工作。

### 7.10 需要新增的前端命令封装
- `showMainWindow()` — 调后端命令把主窗拉到前台
- 后端 `show_main_window` 命令：get_webview_window("main").show().set_focus()

### 7.11 本阶段不做
- ❌ macOS 辅助功能权限引导（等 mac 适配时做）
- ❌ 浮窗拖拽（定位固定右下，不拖）
- ❌ 浮窗多实例（始终只有一个）
- ❌ 浮窗持续播放进度（发送后只显示"已发送"）

---

*阶段 7 设计稿提交时间：2026-07-13，直接实现*

---

## 阶段 8：UI 设计「安墨」+ 多轮修复（实现后补记）

> 本节是代码落地后补写的，记录阶段 7 之后的所有 UI / 体验改动，使文档与代码同步。

### 8.1 「安墨」净面优雅风（应用全局）
- `globals.css` 配色 token：宣纸暖白 `#faf8f4` + 墨色芯 + 暖琥珀 `#b87333` 唯一强调 + 朱印 `#a8513a`
- `font-display` 用 Noto Serif SC / 思源宋体作品牌字"语笺"
- 微动效：消息气泡淡入、抽屉从右滑入、菜单淡入、正在播放琥珀脉动
- 文本选择呈现"朱批"色

### 8.2 音量控件形态演进
- 阶段 4：底部固定工具栏 → 阶段 4.6：悬浮球（hover 展开 / 长按拖拽）
- 悬浮球方案拖拽不稳，**最终回退为右上角按钮**：`VolumeControl` 点击展开含"音量+播放速度"两滑块的卡片
- 合成语速（`tts_speed`）已移除：MiMo 无数值化语速区分，仅留前端精确的 `playback_rate`

### 8.3 右键菜单多次修复（重点记录踩坑）
问题演进及最终方案：
1. 菜单出现在气泡下/输入框下 → 用 `useEffect` 按视窗实测尺寸自动校正位置
2. 菜单被相邻气泡遮挡 → 用 `createPortal` 把菜单挂到 `document.body`，全局 z 9999
3. 切换右键另一气泡时，功能仍指向第一个气泡 → 根因：**用了全屏遮罩 `fixed inset-0` 关菜单**，对第二个气泡右键时鼠标实际落在遮罩上，事件被遮罩拦截，第二气泡的 `onContextMenu` 没正常触发
4. 最终方案：**取消全屏遮罩**，改在 document 上监听 `mousedown` / `contextmenu`：点到菜单外就关闭当前菜单，但不阻止右键事件继续派发到目标气泡，让新气泡正常接管

### 8.4 设置抽屉 + 克隆音色（阶段 6 补记）
- 右侧抽屉 `SettingsDrawer` 从右滑入，纸面卡片
- API Key 输入框 `type="password"` 暗文显示
- 音色下拉：常用预置音色（mimo_default / 冰糖 / 茉莉 / 苏打 / 白桦）+ 克隆音色
- 克隆音色：导入一段 5–10 秒音频样本（mp3/wav ≤10MB），复制到 `voice_samples/`
  - 后端 `import_clone_voice` / `remove_clone_voice` 命令
  - 选中克隆时 TTS 切到 `mimo-v2.5-tts-voiceclone` 模型 + 把样本 base64 塞进 `audio.voice`
  - 每次合成都要传样本，速度比预置音色慢

### 8.5 浮窗修复
- 全局快捷键 `Alt+V`：只响应 `ShortcutState::Pressed`，松开不双触发
- 浮窗发送后自动播放语音（之前只显示"已发送"）

### 8.6 互斥播放
- 主窗持全局唯一 `Audio` 元素，播放新音频前 `pause` 旧的
- 正在播放的消息/收藏用 `playingPath`（audio_path 维度）作 key 高亮，跨视图统一
- `ended` 自然结束时清高亮

### 8.7 在文件夹中显示
- 复用 `tauri-plugin-opener` 的 `revealItemInDir`
- services 加 `getAudioAbsPath` + `revealAudio`
- 消息气泡右键菜单 + 收藏列表每条（hover 出现）都加 📁 入口

### 8.8 跨平台构建辅助
- `src-tauri/.cargo/config.toml` 加 `[http] check-revoke = false`，绕过 Windows SSL 吊销检查报错

### 8.9 已知待办（暂未在文档其它处记录）
- 系统托盘 6.x 未做（关窗=最小化到托盘等）
- macOS 适配 12.x 未做
- 应用图标 0.5 仍用占位
- `settings.hotkey_show_window` 字段已存但快捷键写死 Alt+V，设置面板此字段禁用

### 6.6 呈现方式（确认）
右侧抽屉滑入（从右边滑入卡片 + 遮罩，点遮罩/关闭收回）。

### 6.7 音色选择（确认）
- 下拉只列常用预置中文音色：mimo_default / 冰糖 / 茉莉 / 苏打 / 白桦
- 另加"克隆音色样本"区域：导入一段本地音频 + 起名，首版**只支持一个克隆样本**
- 选 `clone:<样本>` 时，TTS 切到 `mimo-v2.5-tts-voiceclone` 模型，把样本 base64 塞进 `audio.voice`

### 6.8 克隆音色数据流（首版一个样本）

```
设置面板"克隆音色样本"
  弹文件选择器选本地 mp3/wav（≤10MB）
  → 复制到 <data_dir>/voice_samples/clone.<ext>
  → settings 存 clone_voice_name（用户起的名）+ clone_voice_path
  → 音色下拉新增"克隆：xxx"项

TTS 生成时（generate_tts）:
  若 tts_model.startswith("clone:")：
    1. 切 model=mimo-v2.5-tts-voiceclone
    2. 读 clone_voice_path 文件 → base64 → "data:audio/<ext>;base64,..."
    3. 塞 audio.voice = 该 data URI
    4. 正常调用
  否则走预置音色 mimo-v2.5-tts 流程
```

**复杂度提示**：每次合成都要把样本字节 base64 化传给 MiMo（≤10MB+），克隆音色生成比预置慢，这是 MiMo 机制决定的。

### 6.9 Settings 新增字段
- `clone_voice_name: String`（空字符串 = 无克隆样本）
- `clone_voice_path: String`（空字符串 = 无克隆样本）

### 6.10 新增命令
- `import_clone_voice(file_path, name)`：复制文件到 voice_samples/，更新 settings 两字段，广播 settings:changed
- `remove_clone_voice()`：清空两字段 + 删样本文件

### 6.11 MimoEngine 改造
`build_request_body` 加 `model`/`voice` 已支持，新增：当 voice 是 `data:` URI 开头时原样传（不补默认音色），并允许外部传 model id。`generate_tts` 据 tts_model 前缀决定传 clone 流程还是预置流程。

---

## 阶段 6.12 实现顺序
1. 后端 Settings 加两字段 + load/update 支持
2. 后端 import_clone_voice / remove_clone_voice 命令
3. MimoEngine 适配 clone（model+voice data URI）
4. generate_tts 据 tts_model 分支
5. 前端设置抽屉组件（含音色下拉、克隆样本区、API Key、快捷键、关于）
6. 替换右上角⚙️行为打开抽屉
7. 测试 + 构建

---

## 阶段 9：接入 Moss-TTS 作为可选第二引擎

**触发**：v1.0.0 发布后用户希望增加 moss-tts（Mossland API）作为第二可选供应商。
**文档依据**：doc/tts-api/moss-tts.md（已整理完整）。

### 9.1 已核实的关键差异

| 维度 | MiMo（现有） | Moss-TTS（新） |
|---|---|---|
| 端点 | `POST https://api.xiaomimimo.com/v1/chat/completions` | `POST https://api.mosi.cn/v1/audio/speech` |
| 鉴权 | `api-key: <key>` 请求头 | `Authorization: Bearer <key>` 请求头 |
| 文本入参 | `messages[1].content`（role=assistant） | `input: "文本"` 直接字段 |
| 音色 | `audio.voice` 传 id 或 base64 | 只接受 `voice_id`，需先在控制台创建音色 |
| 返回 | JSON → base64 wav | `delivery_method=url` 同步返回含结果 URL 的 JSON |
| 格式 | wav（base64 内嵌） | mp3（默认），URL 下载 |
| 异步 | 不支持 | 支持 `async=true`（首版不做，只用同步） |

### 9.2 设计决策

- **同步 `delivery_method=url`**：POST 后直接拿到音频 URL，再下载存本地。不用异步轮询。
- **voice_id 手动填**：首版不调 `GET /v1/audio/voices`，用户从 moss 控制台拿 voice_id 填入设置。
- **response_format=mp3**：moss 默认格式，HTML5 audio 直接播。
- **Settings 新增两字段**：`moss_api_key`、`moss_voice_id`。`tts_engine` 已有（值 "mimo"|"moss"）。
- **dispatch**：`generate_tts` 据 `settings.tts_engine` 分发。
- **不动**：`tts/traits.rs` trait 接口（本来就为多引擎预留）；MiMo 现有实现。

### 9.3 新增/改动文件

**后端**
- `tts/moss.rs`（**新**）：`MossEngine` 实现——POST 请求构造 → 解析响应拿 URL → 下载音频 → 存 mp3 文件
- `commands/tts.rs`：`generate_tts` 加 match 分发到 MimoEngine / MossEngine
- `storage/types.rs`：Settings 加 `moss_api_key` / `moss_voice_id`
- `storage/settings.rs`：load/update 支持新字段
- `lib.rs`：无改动（命令层已注册 generate_tts）

**前端**
- `types/index.ts`：Settings 接口加 `moss_api_key` / `moss_voice_id`
- `SettingsDrawer.tsx`：
  - 新增「TTS 引擎」下拉选择（MiMo / Moss-TTS）
  - 选 Moss 时显示 moss_api_key + moss_voice_id 输入框
  - 选 MiMo 时显示原有 mimo_api_key 输入框
  - 引擎切换即调 `update_setting("tts_engine", ...)`

**不动**：App.tsx、QuickInput.tsx（它们只调 `invoke("generate_tts")`，后端自动分发）

### 9.4 MossEngine 实现要点

```
MossEngine { api_key, voice_id, data_dir }

generate(text):
  1. POST https://api.mosi.cn/v1/audio/speech
     Authorization: Bearer <api_key>
     body: { model: "moss-tts", input: text, voice_id, response_format: "mp3", delivery_method: "url" }
  2. 响应 JSON 中取音频 URL（字段名待实测确认，先试 audio_url / url / output_url）
  3. 用 reqwest 下载该 URL → 音频字节
  4. 存为 audio/m_<id>.mp3（相对路径）
  5. 返回相对路径
```

### 9.5 待实测确认（做完后跑一次）

- 响应 JSON 中音频 URL 的字段名（文档未给示例响应体）
- moss API 错误格式（403 permission_error / 400 invalid_request_error）
- mp3 文件前端播放是否正常

### 9.6 实现顺序

1. Settings 加两字段 + load/update 支持
2. `tts/moss.rs` 新建 MossEngine
3. `commands/tts.rs` 加分发
4. 前端 types + SettingsDrawer 引擎选择 UI
5. 构建 + 用真实 key 测试

---

## 阶段 10：Moss 音色库管理（持久化 + 增删改）

**触发**：v1.1 Moss 接入后，用户希望维护一个可增删改的音色列表，而非每次手填单个 voice_id。
**决策**：
- 不做音色列表查询接口（用户去 https://mossland.mosi.cn/voice/library 手动查）
- 音色列表**持久化到 settings**，用户在前端增删改（名称 + id）
- 默认预置一个音色：**曼波 / 11d3a27a-0951-4bcd-96ae-ab414f454764**

### 10.1 数据模型

```rust
// types.rs
pub struct MossVoice {
    pub name: String,      // 音色名称（展示用）
    pub voice_id: String,  // 音色 id（Mossland 控制台获取）
}
// Settings 加字段：
//   moss_voices: Vec<MossVoice>   // 音色库，默认 [曼波]
//   moss_voice_id: String          // 当前选中音色 id，默认 = 曼波 id
```

### 10.2 后端改动

- `types.rs`：`MossVoice` struct + `moss_voices` 字段（Default 预置曼波）+ `moss_voice_id` 默认值改为曼波 id
- `settings.rs` load：`moss_voices` 反序列化兜底 default
- `settings.rs` update_setting：新增 `"moss_voices"` 分支，整体替换列表（`serde_json::from_value::<Vec<MossVoice>>`）

### 10.3 前端改动

- `types/index.ts`：`MossVoice` 接口 + Settings 加 `moss_voices`
- `invoke.ts`：`updateSetting` value 类型扩展支持 `MossVoice[]`
- `SettingsDrawer.tsx`：Moss 区重构——
  - 「当前使用音色」下拉（来自 moss_voices，选中即写 moss_voice_id）
  - 「音色库」列表：每行 名称 + id + 编辑 + 删除；底部 添加表单（名称 + id + 添加按钮）
  - 删除当前音色时，自动把 moss_voice_id 切到剩余第一个
  - 加「前往 Mossland 音色库查询」链接

### 10.4 实现顺序
1. types.rs MossVoice + 字段 + Default
2. settings.rs load/update 支持
3. 前端 types + invoke 类型
4. SettingsDrawer 音色库 UI
5. 构建 + 测试

