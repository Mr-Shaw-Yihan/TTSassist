## v1.8.7 — 修三处体验问题

> **字幕**与**遥控 App** 仍处于测试阶段，欢迎反馈 bug。

### 修复

- **点侧边栏「遥控」会闪一个黑色控制台窗口**：防火墙放行状态探测与局域网 IP 枚举改为隐藏窗口执行，不再闪框
- **「字幕」入口可能凭空消失**：此前只要 ASR 插件未加载成功就整体隐藏该入口，用户看到的现象就是"字幕识别功能没了"（而插件页里它显示正常）。现在入口常驻，无可用引擎时由字幕页内提示引导去「插件」页安装
- **更新说明显示乱码**：发布脚本取更新说明时，文本被按 GBK 误解码后写进升级清单，导致 v1.8.6 的更新详情是一堆乱码。已改为直接读取仓库内的说明文件，并加了一道"说明里没有中文就告警"的拦截。本条只影响说明文本，从 v1.8.7 起显示正常

### 升级说明

- 软件内直接升级：设置 → 关于 → 检查更新 → 立即升级（v1.8.6 起安装完成后会自动重启应用）
- 静默安装过程中可能弹一次防火墙提权（UAC）窗口，属正常现象

### 反馈

Issues 或 QQ 群均可，请尽量带上：系统版本、音频设备情况（字幕）、网络环境（遥控 / 升级失败时的通道与报错文案）、复现步骤。日志可在「设置」页开启诊断日志后一并附上。
