## v1.8.5 — 支持软件内一键升级；更新说明改为排版显示

> ⚠️ **字幕**与**遥控 App** 两项功能仍处于测试阶段，功能可用但未经多环境验证，欢迎反馈 bug。

### 新增

- **软件内直接升级**：发现新版本后不必再跳到浏览器，在应用内即可完成下载与安装
  - 国内网络优先从 Gitee 取包，Gitee 不通时自动改用 GitHub，无需手动选择
  - 两条通道都失败时，会给出 QQ 群号（点击即可复制）与两条手动下载直链，仍可在群文件里拿到同一版本的安装包
  - 下载过程带进度显示，完成后可取消；落地后强制校验文件完整性（SHA-256），校验不过不会执行
  - **不会自动打断你**：下载校验完成后需你点「立即安装并重启」才真正安装；若字幕监听或引擎安装正在进行，会先弹出确认
- 设置「关于」页同样可以直接完成升级，不再只是一个跳转链接

### 变更

- 更新说明由 Markdown 源码改为排版渲染（标题 / 列表 / 引用 / 行内代码），长说明的可读性明显提升
- 关闭升级弹窗不会打断已经开始的下载，进度可在「关于」页继续查看

### 升级说明

- **从 v1.8.4 升级到本版仍需手动下载安装一次**（v1.8.4 还没有软件内升级能力）；装上 v1.8.5 之后，后续版本都能在软件内一键升级
- 安装器仍会自动放行所需端口（TCP 45271 / UDP 5353）；静默安装过程中可能弹一次防火墙提权（UAC）窗口，属正常现象

### 反馈

Issues 或 QQ 群均可，请尽量带上：系统版本、音频设备情况（字幕）、网络环境（遥控 / 升级失败时的通道与报错文案）、复现步骤。日志可在「设置」页开启诊断日志后一并附上。
