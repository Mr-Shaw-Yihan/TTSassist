## v1.8.2 — 遥控体验完善：配对更简单、发现更可靠

> PC 端与遥控 App 版本对齐 1.8.2。

### 变更

- **配对码全面移除**：PC 设置面板不再显示配对码，配对唯一路径为「App 点发现的 PC → PC 弹窗点允许」（物理在场模型，更安全也更简单）；旧版 App 的配对码输入此前已移除，协议收到配对码报文将明确拒绝
- **App 不再自动连接**：每次启动进配对页，由你在「发现的电脑」里手动点击连接（已配对的 PC 点击后免弹窗直达）
- **发现列表设备条目**改为磨砂哑金按钮样式

### 修复

- **无法自动发现 PC（重要）**：子网扫描的 HTTP 复核会误杀真实 PC（WS 服务端对裸 GET 不回可读响应）——改为 WebSocket 升级握手复核（101 才算命中），同时修复 VPN TUN 环境的假设备误报（172.19.x 等）
- 子网扫描改分批并发（防淹 WiFi 栈）、超时放宽至 700ms；内置引导扫描目录与 resource_dir 语义解耦

### 遥控 App（Android，1.8.2）

- 功能同上；内含应用内检查更新（此前 1.8.1 加入），后续版本可直接在 App 内更新

### 升级说明

- PC 端直接安装覆盖；App 建议先升到本版（以后可在 App 内一键更新）
- 安装器自动放行所需端口（TCP 45271 / UDP 5353），无需手动配置
