## v1.8.4 — 新增音频转字幕；遥控迁入本体

> ⚠️ **字幕**与**遥控 App** 两项功能均处于测试阶段，功能可用但未经多环境验证，欢迎反馈 bug。

### 新增

- **音频转字幕（测试中）**：监听本机正在播放的声音，自动把语音识别成字幕，显示在置顶、鼠标穿透的浮窗上——打游戏 / 开会 / 看视频时不抢焦点、不挡操作。
  - 入口：侧边栏「字幕」标签（需已安装 ASR 识别插件，如内置的 MiMo ASR）
  - 只对检测到的真实语音发起识别，静音片段不花钱
  - 全局快捷键 `Alt+M` 一键开 / 关监听与浮窗（可改键，保存时自动检测冲突）
  - 可调项：识别引擎与语言、语音检测灵敏度、浮窗位置 / 字号 / 透明度 / 同屏条数 / 自动淡出
  - 支持最近会话回看与导出 txt
  - 识别有 2–5 秒延迟属正常，不是卡住

### 变更

- **手机遥控器由插件迁入本体常驻**：不再需要单独安装 `lan-remote` 插件，装好本体即可被手机遥控；升级时自动清理旧插件残留
- 侧边栏顺序调整为：消息 / 收藏 / 语音 / 字幕 / 插件 / 遥控 / 设置

### 修复（尝试性）

- **遥控 App 连不上电脑**：App 侧 WebSocket 改为强制直连，绕过手机系统代理与 VPN，缓解「PC 已开启遥控但 App 始终连不上」

### 遥控 App（Android，1.8.4）

- 含上述连通性修复；APK 见 `remote-v1.8.4` Release 的 `voiceassist-remote-1.8.4-arm64.apk`
- **仍处于测试阶段**：配对、发现、遥控稳定性都可能有坑，反馈时请附手机型号 / 系统版本 / 是否开 VPN

### 升级说明

- PC 端直接安装覆盖；已装过 `lan-remote` 插件的用户升级后该插件会被自动移除，属预期行为
- 安装器自动放行所需端口（TCP 45271 / UDP 5353），无需手动配置

### 反馈

Issues 里请尽量带上：系统版本、音频设备情况（字幕）、网络环境（遥控）、复现步骤。日志可在「设置」页开启诊断日志后一并附上。
