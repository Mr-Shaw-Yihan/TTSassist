## v1.8.0 — 遥控公测修复：内置插件自动安装 + 版本对齐

> 遥控 App 与 PC 端版本号从本版起对齐（1.8.0）。

### 修复

- **minimax 插件在 1.7.9 上无法加载**：minimax-tts / minimax-tts-global v0.2.1 要求宿主 ≥1.8.0，宿主版本号已同步升至 1.8.0
- **全新安装后「手机遥控」插件检索不到**：安装包内置的插件 zip 此前只复制不注册，插件页看不到。宿主新增内置插件引导——启动时自动安装注册包内插件（幂等，老用户升级安装包同样生效）
- lan-remote 插件最低宿主要求修正为 1.8.0 随包对齐

### 遥控 App（Android，1.8.0）

- 功能与 1.7.9 一致（免码配对、断线自动重连、mDNS+子网扫描双通道发现、黑金 UI），版本号随本体对齐
- 安装：下载下方 `voiceassist-remote-1.8.0-arm64.apk`（允许「未知来源」），手机与 PC 同一局域网，首次连接在 PC 点「允许」

### 安装器改进

- NSIS 安装完成时自动放行遥控所需端口（TCP 45271 / UDP 5353，覆盖专用/公用网络，单次 UAC），新用户零手动配置；卸载时自动清理
